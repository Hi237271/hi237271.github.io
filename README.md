Updates and renders fantasy football data 
Player data is contained in out.json. Run main.py to update out.json
main.py runs in a pipenv environment. Use "pipenv run python main.py" to run main
index.html uses the player data in out.json to render the fantasy football site
